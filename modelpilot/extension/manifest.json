{
  "manifest_version": 3,
  "name": "ModelPilot for Claude",
  "version": "0.1.0",
  "description": "Pre-send model guidance and savings estimates on claude.ai, powered by your local ModelPilot gateway. Advisory only — never changes anything.",
  "permissions": ["storage"],
  "host_permissions": ["http://127.0.0.1:8400/*"],
  "background": { "service_worker": "background.js" },
  "content_scripts": [
    {
      "matches": ["https://claude.ai/*"],
      "js": ["content.js"],
      "run_at": "document_idle"
    }
  ]
}

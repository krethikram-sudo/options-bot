// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  site: 'https://krethikram-sudo.github.io',
  base: '/options-bot',
  build: {
    assets: 'assets',
  },
});

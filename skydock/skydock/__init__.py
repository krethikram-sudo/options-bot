"""Skydock — vehicle-deployed drone simulation for AV training data collection.

Models the MVP spec v0.1: operations (Section 9.1), data pipeline (9.2),
and unit economics (9.3) wired into one full-system sim (9.4).
"""

__version__ = "0.1.0"

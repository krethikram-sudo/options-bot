name: scopepilot-ci

# ScopePilot is the primary product; its tests must gate changes to it.
# Path-filtered so it only runs when scopepilot/ (or this workflow) changes.
on:
  push:
    paths:
      - "scopepilot/**"
      - ".github/workflows/scopepilot-ci.yml"
  pull_request:
    paths:
      - "scopepilot/**"
      - ".github/workflows/scopepilot-ci.yml"

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      # modelpilot/requirements pulls in pytest + httpx + fastapi + anthropic, so
      # the gateway-observer-seam test exercises the real gateway rather than
      # self-skipping. ScopePilot itself has no third-party deps.
      - run: pip install -r modelpilot/requirements.txt
      - run: python -m pytest scopepilot/tests/ -q

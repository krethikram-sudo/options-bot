fastapi>=0.110
uvicorn[standard]>=0.27
# Encrypts stored connector tokens at rest (Fernet, keyed off CONSOLE_SECRET).
cryptography>=42.0
# Optional: enables live billing. The console runs without it (convert-to-paid
# records the plan; metering still accrues) and turns Stripe on once it's present
# and STRIPE_SECRET_KEY is set.
stripe>=8.0
# Passkeys / WebAuthn (FIDO2) — phishing-resistant MFA. The vetted library that does
# the attestation/assertion verification (webauthn_box.py wraps it). Console degrades
# gracefully without it (passkey features report unavailable).
webauthn>=2.0
